

directory_source  = '../../src'
directory_example = '../../example'

env = Environment( )
env.Append( CPPPATH  = [ directory_source ] )
env.Append( CXXFLAGS = [ "/EHsc" ] )

 

list = []
list += [ directory_example + '//reaction.cpp' ]

env.Library( 'libw', list  )
